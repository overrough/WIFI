# ── Web Framework ──────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.1.3
python-multipart>=0.0.9

# ── Database ───────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.36
aiosqlite>=0.20.0             # SQLite async driver (zero install, built into Python)
asyncpg>=0.29.0               # only needed if switching to Postgres later
alembic>=1.13.0

# ── Cache ──────────────────────────────────────────────────
redis[asyncio]>=5.0.0

# ── LLM & Agent Framework ──────────────────────────────────
langchain>=0.3.7
langchain-anthropic>=0.3.3
langchain-openai>=0.2.9
langchain-community>=0.3.7
langchain-ollama>=1.1.0          # required for tool-calling on local Ollama models
langgraph>=0.2.53
anthropic>=0.40.0
openai>=1.58.0

# ── Vector & Embeddings ────────────────────────────────────
chromadb>=0.5.18
sentence-transformers>=3.3.1

# ── Google APIs (Calendar + Gmail, free via OAuth) ────────────────────────────
google-auth>=2.30.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.130.0

# ── Search ─────────────────────────────────────────────────
duckduckgo-search>=6.0.0      # free, no API key required
tavily-python>=0.3.9
httpx>=0.27.0

# ── Browser Automation ─────────────────────────────────────
playwright>=1.48.0
# After installing: python -m playwright install chromium

# ── Computer Automation ────────────────────────────────────
mss>=9.0.2           # cross-platform screenshots
psutil>=6.0.0        # system info (CPU, memory, disk)

# ── Auth & Config ──────────────────────────────────────────
python-dotenv>=1.0.1
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ── Utilities ──────────────────────────────────────────────
pytz>=2024.2
aiofiles>=24.1.0
tenacity>=8.5.0

# ── Background routines (proactive layer) ──────────────────
apscheduler>=3.10.4
